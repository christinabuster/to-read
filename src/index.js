import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// ReactDOM is render the app component into the element with the id "root"
ReactDOM.render(<App />, document.getElementById('root'));
// The app component that is being loaded is app.js
